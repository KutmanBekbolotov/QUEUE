# Scripts

Utility scripts for local development and maintenance should live here. The main local workflow is currently exposed through the root `Makefile`.

- `live-smoke.sh`: end-to-end Docker smoke for PostgreSQL, RabbitMQ, Spring, NestJS auth, JWT compatibility, queue, booking, and external middleware routes.
